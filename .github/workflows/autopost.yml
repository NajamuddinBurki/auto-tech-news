# after checkout & setup Python
- name: Install dependencies
  run: |
    pip install feedparser playwright schedule
    playwright install chromium

- name: Run auto poster
  run: |
    python main.py
